using System.Collections;
using System.Collections.Generic;
using UnityEngine;

using UnityEngine.SceneManagement;

public class Lobby : MonoBehaviour
{
    void Start()
    {
        // Turn CURSOR back on when returned/kicked to Lobby.
        Cursor.visible = true;
    }

    void Update()
    {
        
    }
}
